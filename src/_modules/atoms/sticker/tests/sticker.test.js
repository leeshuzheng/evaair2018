'use strict';

import Sticker from '../sticker';

describe('Sticker View', function() {

  beforeEach(() => {
    this.sticker = new Sticker();
  });

  it('Should run a few assertions', () => {
    expect(this.sticker);
  });

});
